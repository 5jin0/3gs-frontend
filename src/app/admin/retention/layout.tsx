import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "리텐션",
};

export default function AdminRetentionLayout({ children }: { children: React.ReactNode }) {
  return children;
}
