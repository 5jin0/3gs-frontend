import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "접속·코호트",
};

export default function AdminCohortsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
