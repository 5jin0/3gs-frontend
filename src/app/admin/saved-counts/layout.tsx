import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "유저별 저장",
};

export default function AdminSavedCountsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
