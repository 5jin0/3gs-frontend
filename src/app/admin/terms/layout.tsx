import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "용어",
};

export default function AdminTermsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
